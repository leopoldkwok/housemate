App.BillRoute = Ember.Route.extend({
    // renderTemplate: function() {
    //     this.render('bill',{ outlet: 'form', controller: 'App.BillController' });
    // },
    // model: function(params) { return this.store.find('bill', params.id)}
})